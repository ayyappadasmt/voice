# WebSocket URL of the FastAPI backend voice endpoint.
# Local dev (backend on :8000):
NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws/voice
# In production use wss:// behind TLS, e.g.:
# NEXT_PUBLIC_WS_URL=wss://your-domain.example.com/ws/voice
